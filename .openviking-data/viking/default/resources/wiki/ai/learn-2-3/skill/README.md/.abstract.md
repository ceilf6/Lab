这份目录主要是一篇关于 **Claude Code 技能调用机制** 的说明性笔记，围绕 **用户输入、系统提示词（system prompt）、模型决策、JSON 工具调用、技能内容读取** 之间的完整交互流程展开。内容重点解释了初始化请求中的 **Tools / skill 定义**、**CLAUDE.md 与 system-reminder 的拼接方式**、以及模型何时决定调用本地技能、何时返回 UI 的流程关系。它还讨论了 **“渐进式披露”** 的技能读取策略、技能名称与调用格式要求，以及系...