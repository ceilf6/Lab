这个目录主要是一份关于 **HTML5 媒体元素** 的简明教程/学习笔记，核心内容围绕 **`video`** 与 **`audio`** 标签的基础用法、**布尔属性**（如 `controls`、`autoplay`、`muted`、`loop`）的规则，以及 **浏览器兼容性** 与多格式资源降级方案。文档强调如何通过多个 `source` 为视频提供 `mp4`、`webm` 等备选格式，并用提示文本增强可用性。整体内容适合 **前端入门者、HTML5 学习者**，以及需要实现音视频播放与跨...