`core` 目录是一份关于 **HTML 实体字符（HTML Entity）** 的简明参考资料，重点说明如何在网页中显示特殊符号，以及如何用实体转义避免字符被 HTML 误解析。内容覆盖实体字符的两种常见写法：`&单词;` 与 `&#数字;`，并给出常见示例，如小于号 `&lt;`、大于号 `&gt;`、空格 `&nbsp;`、non-breaking space 和版权符号 `&copy;`。这份资料适合 **前端初学者、HTML 编写者、网页内容编辑者**，以及需要快速查找实体字符写法的开发者...