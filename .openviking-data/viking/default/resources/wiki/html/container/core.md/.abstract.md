`core` 目录是一份关于 HTML 语义化容器元素的参考说明文档，重点讲解常见结构标签的用途、基本语义以及它们在页面组织中的角色。内容覆盖 `div`、`header`、`footer`、`article`、`section`、`aside`、`nav` 等核心标签，强调 `div` 主要用于布局但不带语义，而其他元素则用于提升文档结构表达、可读性与可维护性。同时也提到这些容器元素会影响 CSS 样式部署，且浏览器通常不会为其提供默认样式。适合前端初学者、HTML/CSS 学习者，以及需要快速查阅...