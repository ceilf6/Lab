这是一份面向 **wordMCP** 相关功能的极简操作提示文档，核心围绕 **环境准备** 与 **后端服务启动** 展开。它明确说明在使用 wordMCP 之前，需要先进入 `agent/_MCP/wordMCP` 目录并执行 `./start.sh` 启动后端服务，强调依赖进程必须先运行这一前置条件。内容适合用于 **快速上手**、**部署前检查清单**、**本地开发指南** 或 **集成接入说明**，读者通常是需要运行 MCP/Word 服务的开发者、运维人员或系统集成使用者，并默认具备基本的...