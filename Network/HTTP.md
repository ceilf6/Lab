./HTTP各版本.md

./HTTP2Priority

# HTTP限定了必须是从客户端发送请求到服务器，导致不利于实时通信

明明传输层 TCP 是**全双工的，双方随时都能发数据**
但是应用层 HTTP 限定了必须从客户端先发请求

这是因为

1. HTTP 诞生时的目标是简单可靠地获取资源，并适配代理、防火墙、无状态扩展，而不是实时双向通信
2. 服务器无法知道客户端什么时候准备好（客户端可能还在解析上一条数据
3. 网络中间设备像代理服务器、缓存服务器、防火墙需要 HTTP 是可预测的