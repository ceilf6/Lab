const remoteAdd = () => {


    return Promise.
}

const target = [];

const add = (...inputs) => {
    let ans = 0;
    const L = inputs.length;
    if (L === 1) return inputs[0]
    else if (L === 2) {

    } else {

    }
    return ans;
}